function getGreeting1(name) {
return 'Hello ' + name + '!';
}

console.log(getGreeting1("dhufr"));

        // write same code with diffrent function syntix

const getGreeting2 = function(name) {
    return 'Hello ' + name + '!';
};

console.log(getGreeting2("dhufr"));

        // write same code with an  arrow function
    
const getGreeting3 = (name) => {
    return 'Hello ' + name + '!';
};

console.log(getGreeting3("dhufr"));

        // DONE BY DHUFR